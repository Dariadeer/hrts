{
  "name": "hrts",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node out/server.js",
    "build": "tsc --build --clean && tsc && npm run copy-files",
    "copy-files": "cp src/public/*.html out/public && cp src/public/*.css out/public",
    "quick-start": "npm run build && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
